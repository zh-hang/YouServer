# YouServer

## 部署

### 创建虚拟环境

```shell
python3 -m venv venv
. venv/bin/activate
```

### 安装flask

```python
pip install Flask
```

### 运行

```shell
export FLASK_APP=app.py
flask run --host=0.0.0.0
```

